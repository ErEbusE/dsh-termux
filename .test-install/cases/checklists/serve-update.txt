1) 版本确实变了: 启动时打印的 dsh 版本 == 本次更新要求的那个目标
   (没变 = 更新没生效, 本项直接失败; 变得比要求还新/还旧都是失败)

2) 页面能打开, 标题是 DeepSeek Harness
   (打印的 URL 是一次性握手 ?token= -> 303 -> 会话 cookie)

3) 更新前就存在的用户数据仍在: 沙箱 HOME 下的会话/设置没有被更新过程清掉
   (这是更新链路与"重装"的分界线)

4) 新建会话并发送一条消息, agent 能回复
   (沙箱默认无真实凭据, 提示缺 API Key 属预期; 未配凭据时本项只能标「未实测」)

5) 让 agent 写一个文件再读回来, 确认落点在沙箱工作区里

6) 让 agent 在 bash 工具里执行 mktemp -d 与 echo x > "$TMPDIR/t" && cat "$TMPDIR/t":
   预期都成功且落在沙箱 tmp/ 里 —— 更新后补丁集必须仍然生效

7) 浏览器交接: 启动时自动弹出的那次就是它; 没弹出就手动打开 dsh 打印的 URL 并说明
   (serve 默认不插桩, 不对"弹没弹"下结论——以你看到页面为准)

8) 边界检查: 全程本地正在运行的 dsh runtime 不受影响 (~/.dsh 与 http://127.0.0.1:3080)

9) Ctrl-C 退出后, 本地原来的 dsh web 仍能正常打开
