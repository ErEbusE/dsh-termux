1) 页面能打开, 标题是 DeepSeek Harness
   (打印的 URL 是一次性握手 ?token= -> 303 -> 会话 cookie)

2) 真实聊天可用: 新建会话、发送消息、agent 完整回复一轮
   本项需要凭据 —— 用 --with-creds 启动(复制本地 ~/.dsh 凭据), 或直接在沙箱 UI 里填 API Key;
   两者都没有时本项只能标「未实测」, 不得标通过

3) 让 agent 写一个文件再读回来, 确认落点在沙箱工作区里

4) 让 agent 在 bash 工具里执行 mktemp -d 与 echo x > "$TMPDIR/t" && cat "$TMPDIR/t":
   预期都成功且落在沙箱 tmp/ 里

5) 浏览器交接: 启动时自动弹出的那次就是它; 没弹出就手动打开 dsh 打印的 URL 并说明
   (serve 默认不插桩, 不对"弹没弹"下结论——以你看到页面为准)

6) 边界检查: 全程本地正在运行的 dsh runtime 不受影响 (~/.dsh 与 http://127.0.0.1:3080)

7) Ctrl-C 退出后, 本地原来的 dsh web 仍能正常打开
